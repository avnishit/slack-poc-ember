{{primary-sidebar userId=model.user convs=model.convs}}
<div class="container">
	<div class="row">
    <div class="Absolute-Center is-Responsive">

        <div class="col-md-12 cpl-lg-8">
  <p style="border-bottom:1px #c5c5c5;">
    Account Access Needed<br>
		Just Enter a username
  </p>
  <div id="form-olvidado">
  <form {{action 'login' on='submit'}}>
      <fieldset>
        <div class="form-group input-group">
          <span class="input-group-addon">
            @
          </span>
          {{input value=username class='form-control' placeholder='Name' required=''}}
        </div>
        <div class="form-group">
          <button type="submit" class="btn btn-primary btn-block">
            Access
          </button>
        </div>
      </fieldset>
    </form>
  </div>
</div>
</div>
	</div>
</div>


{{outlet}}
